{
  "name": "@kithkit/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Kithkit CLI — init, install, search, update.",
  "bin": {
    "kithkit": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/__tests__/**/*.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "MIT"
}
